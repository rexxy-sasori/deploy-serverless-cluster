---
apiVersion: eventing.knative.dev/v1
kind: Trigger
metadata:
  name: network-trigger-java
spec:
  broker: kafka-broker
  filter:
    attributes:
      type: network
      source: curl
  subscriber:
    ref:
      apiVersion: serving.knative.dev/v1
      kind: Service
      name: network-java
