---
kind: Service
apiVersion: serving.knative.dev/v1
metadata:
  name: graph-mst-java
spec:
  template:
    spec:
      containers:
        - image: fusion-108:5000/graph-mst:native
          name: benchmark
          env:
            - name: TYPE
              value: "printRetValTypeU"
